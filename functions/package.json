{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && firebase emulators:start --only functions",
    "serve": "nodemon -e ts --exec npm run dev",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "typescript": "^5.6.0"
  },
  "private": true
}
